#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h> /* for va_list */
#include <stddef.h>
#include <stdio.h>

/* Function prototypes */
int _printf(const char *format, ...);
int _putchar(char c);
int _puts(char *str);

#endif /* MAIN_H */

